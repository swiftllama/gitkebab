--- src/CMakeLists.txt.orig	2021-10-31 15:48:36.513847516 +0100
+++ src/CMakeLists.txt	2021-10-31 15:46:10.614351508 +0100
@@ -218,9 +218,11 @@
 	LIST(APPEND LIBGIT2_LIBS ${LIBSSH2_LIBRARIES})
 	LIST(APPEND LIBGIT2_PC_LIBS ${LIBSSH2_LDFLAGS})
 
+	set(HAVE_LIBSSH2_MEMORY_CREDENTIALS TRUE)
-	CHECK_LIBRARY_EXISTS("${LIBSSH2_LIBRARIES}" libssh2_userauth_publickey_frommemory "${LIBSSH2_LIBRARY_DIRS}" HAVE_LIBSSH2_MEMORY_CREDENTIALS)
 	IF (HAVE_LIBSSH2_MEMORY_CREDENTIALS)
+	  SET(GIT_SSH_MEMORY_CREDENTIALS 1)
+        ELSE()
+          MESSAGE(FATAL_ERROR " HAVE_LIBSSH2_MEMORY_CREDENTIALS is false, libssh2 will be built without memory credentials")
-		SET(GIT_SSH_MEMORY_CREDENTIALS 1)
 	ENDIF()
 ELSE()
 	MESSAGE(STATUS "LIBSSH2 not found. Set CMAKE_PREFIX_PATH if it is installed outside of the default search path.")
