diff --git a/src/libssh2_priv.h b/src/libssh2_priv.h
index 9b8866d..a4dfdc5 100644
--- a/src/libssh2_priv.h
+++ b/src/libssh2_priv.h
@@ -55,6 +55,15 @@
 /* platform/compiler-specific setup */
 #include "libssh2_setup.h"
 
+/* GITKEBAB PATCH - errno is needed by the EINTR retry change in
+ * session.h. iOS does not pull <errno.h> in transitively. */
+#ifdef __APPLE__
+#include "TargetConditionals.h"
+#if TARGET_OS_IPHONE
+#include <errno.h>
+#endif
+#endif
+
 #include <stdio.h>
 #include <string.h>
 #include <time.h>
